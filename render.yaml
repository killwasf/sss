services:
  - type: web
    name: mt5-mt4-bridge
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    instanceCount: 1
    instanceType: free
    envVars: []
